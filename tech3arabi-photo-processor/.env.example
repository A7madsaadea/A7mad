# Google Drive Service Account credentials (base64 encoded JSON)
# To generate: base64 -i credentials.json | tr -d '\n'
GOOGLE_CREDENTIALS_B64=

# Flask secret key (generate a random string)
SECRET_KEY=your-secret-key-here
